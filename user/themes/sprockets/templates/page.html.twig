{% extends 'partials/base.html.twig' %}

{% block content %}
	{% if page.header.showcase.display %}
    {% include 'widgets/showcase.html.twig' %}
  {% else %}
    {% if page.header.carousel %}
      {% include 'widgets/slider.html.twig' %}
    {% else %}
      <header class="row">
        <h1 class="page-title">{{ page.title}}</h1>
      </header>
    {% endif %}
	{% endif %}
	<article class="row">
		{{ page.content }}
	</article>
{% endblock %}
