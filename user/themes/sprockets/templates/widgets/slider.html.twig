<ul id="hero-slider">
  {% for image in page.header.slider %}
    <li>
      <a href="{{ image.anchor }}">
        <img src="{{ page.url }}{{ image.name }}" alt="{{ image.caption }}">
      </a>
    </li>
  {% endfor %}
</ul>
